spring:
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: always

  jpa:
#    generate-ddl: false
    generate-ddl: true
    hibernate:
      ddl-auto: create
      #ddl-auto: create-drop

    #show-sql: true

logging:
  level:
    ROOT: INFO