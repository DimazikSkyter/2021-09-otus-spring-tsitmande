logging:
  level:
    root: INFO
spring:
  datasource:

#    username: sa
#    password: password
    url: jdbc:h2:mem:testdb
  h2:
    console:
      path: /h2
      settings:
        web-allow-others: true
      enabled: true
  batch:
    jdbc:
      initialize-schema: embedded